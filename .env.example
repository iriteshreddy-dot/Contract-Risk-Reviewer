# Copy to .env and fill in. The system runs WITHOUT a key (keyword-only mode),
# but an Anthropic key unlocks nuanced LLM classification and rewrites.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5

# HTTP server port
PORT=3000

# SQLite database path (default: ./data/reviews.db)
DB_PATH=
